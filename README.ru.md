# Иван Бут — Портфолио Full Stack Engineer

> Личный сайт-портфолио · Один HTML-файл · Без сборщиков · €100/час

---

## 🌍 Как открыть

Открой `portfolio.html` в любом браузере. Всё, готово.

---

## 📁 Структура файлов

```
.
├── portfolio.html     ← Весь сайт (HTML + CSS + JS в одном файле)
├── README.md          ← Документация на английском
└── README.ru.md       ← Этот файл (русский)
```

Намеренно **без зависимостей** — никакого `package.json`, `node_modules`.
Стили, анимации, разметка — всё в одном файле.

---

## 🧱 Секции сайта

| Секция | ID / Класс | Описание |
|---|---|---|
| Навигация | `<nav>` | Фиксированный топ-бар с ссылками и статусом |
| Hero | `.hero` | Полноэкранное вступление с анимированным текстом |
| Статистика | `.stats-bar` | 4 ключевые цифры (счётчики с анимацией) |
| Стек | `#stack` | 6 карточек категорий с тегами технологий |
| Проекты | `#projects` | 4 карточки проектов с описанием и стеком |
| Опыт | `#experience` | Временная шкала карьеры с достижениями |
| Контакт | `#contact` | Призыв к действию — почта, LinkedIn, GitHub |
| Футер | `<footer>` | Копирайт + индикатор открытости к работе |

---

## 🎨 Дизайн-система

### Цвета (CSS-переменные в `:root`)

| Переменная | Значение | Использование |
|---|---|---|
| `--bg` | `#080810` | Основной тёмный фон |
| `--surface` | `#0e0e1a` | Фон карточек и секций |
| `--accent` | `#00ff88` | Зелёный акцент — главный выделяющий цвет |
| `--text` | `#e8e8f0` | Основной текст |
| `--muted` | `#6b6b85` | Вторичный / подсказочный текст |
| `--border` | `rgba(255,255,255,0.07)` | Едва заметные разделительные линии |
| `--glow` | `0 0 40px rgba(0,255,136,0.15)` | Зелёная тень-свечение |

Хочешь изменить акцентный цвет по всему сайту — поменяй только `--accent`.

### Шрифты

| Шрифт | Использование | Начертания |
|---|---|---|
| **Syne** | Заголовки, лого, названия | 400, 700, 800 |
| **DM Mono** | Основной текст, теги, подписи | 400, 500 (курсив) |

Загружаются с Google Fonts. При первом открытии нужен интернет (после кешируются).

---

## ⚙️ Что делает JavaScript

### 1. Кастомный курсор
Двухслойный курсор:
- **Точка** (`.cursor`) — мгновенно следует за мышью
- **Кольцо** (`.cursor-ring`) — следует с плавной задержкой через **lerp-интерполяцию**

Формула lerp: `pos = pos + (цель - pos) * 0.12`

При наведении на ссылки и карточки → курсор расширяется.

### 2. Scroll Reveal (появление при прокрутке)
Все элементы с классом `.reveal` начинают невидимыми (`opacity: 0`, `translateY: 20px`).

`IntersectionObserver` следит за ними. Когда элемент попадает в viewport (10% видно), добавляется класс `.visible` → запускается CSS-переход.

Каскадный эффект: каждый следующий элемент получает задержку `+60мс`.

### 3. Анимированные счётчики
Цифры (7+, 40+, 12M, €100) считают от нуля при попадании полосы статистики в viewport.

Используется кривая **easeOutCubic**: `eased = 1 - (1 - progress)³`
Быстро в начале, замедляется к концу — выглядит естественно.

---

## ✏️ Как кастомизировать

Ищи в HTML-файле комментарии `CHANGE ME` / `ИЗМЕНИТЬ` — они отмечают все персональные данные.

### Быстрый чеклист:

- [ ] Тег `<title>` — название страницы во вкладке браузера
- [ ] `.nav-logo` — твои инициалы (например, `IB/`)
- [ ] `.hero-title` — твоё имя и фамилия
- [ ] `.hero-desc` — личный питч (2–3 предложения о себе)
- [ ] `.hero-label` — слоган + ставка
- [ ] Значения в массиве JS `values[]` + в HTML
- [ ] Карточки стека — добавь/удали теги технологий
- [ ] Карточки проектов — замени на реальные проекты
- [ ] Пункты временной шкалы — реальная история работы
- [ ] Контактные ссылки — `href="mailto:..."`, URL LinkedIn, GitHub
- [ ] Копирайт в футере — имя + год

---

## 🎯 Производительность

- **Никаких JS-фреймворков** — чистый Vanilla JS
- **Никаких внешних CSS** — всё внутри `<style>`
- **Google Fonts** — только 2 шрифта, загружаются асинхронно с `display=swap`
- **CSS-анимации** — GPU-ускорение (`transform`, `opacity`)
- **IntersectionObserver** — ленивая проверка, останавливается после первого срабатывания (`unobserve`)
- **requestAnimationFrame** — плавные 60fps для курсора и счётчиков

---

## 📱 Адаптивность

| Точка разрыва | Изменения |
|---|---|
| `> 768px` | Полный макет — все колонки, ссылки навбара видны |
| `≤ 768px` | Ссылки навбара скрыты, статистика в 2 колонки, уменьшены отступы |

> **TODO:** Добавить бургер-меню для мобильного навбара.

---

## 🌐 Поддержка браузеров

Работает во всех современных браузерах:
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

> Примечание: `backdrop-filter: blur()` не поддерживается в Firefox < 103.
> Навбар всё равно будет работать, просто без эффекта размытия.

---

## 📋 Как пользоваться

### Вариант 1 — просто открыть
```
Двойной клик по portfolio.html → открывается в браузере
```

### Вариант 2 — локальный сервер (рекомендуется)
```bash
# Python
python -m http.server 3000

# Node.js (если установлен)
npx serve .

# VS Code → расширение "Live Server" → кнопка "Go Live"
```

### Вариант 3 — деплой на хостинг
Загрузи `portfolio.html` на любой хостинг:
- **GitHub Pages** — бесплатно, автоматически
- **Netlify** — перетащи файл на netlify.com
- **Vercel** — `vercel deploy`
- **Любой VPS/сервер** — положи в `/var/www/html/`

---

## 💡 Советы по кастомизации

### Сменить цвет акцента
```css
/* В :root измени одну переменную */
--accent: #ff6b35;   /* оранжевый */
--accent: #7b5ea7;   /* фиолетовый */
--accent: #0099ff;   /* синий */
```

### Добавить проект
Скопируй блок `.project-card` в секции `#projects` и замени:
- `pv-1/2/3/4` → добавь `pv-5` с новым градиентом в CSS
- `project-badge` → тип проекта
- `project-title`, `project-desc` → твои данные
- Теги в `.project-stack`

### Добавить технологию в стек
Просто добавь `<span class="tag">НазваниеТехнологии</span>` в нужную `.stack-tags`.

---

## 📄 Лицензия

Свободно использовать, изменять и деплоить для личного портфолио.
Указание авторства приветствуется, но не обязательно.

---

## 👤 Автор

**Иван Бут** — Full Stack Engineer
- 📧 ivan@but.dev
- 💼 [LinkedIn](https://linkedin.com/in/ivanBut)
- 🐙 [GitHub](https://github.com/ivanBut)
- 💰 Ставка: €100/час · Удалённо · Доступен сейчас
